apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-hello.fullname" . }}-test-connection"
  labels:
    {{- include "java-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
